-----------------------------------
Downloads
-----------------------------------
h1(FirstChild#stable). Stable Version: %(version)<template:text eval="release"/>%

Not yet available

h1(#prerelease). Prerelease Version: %(version)<template:text eval="prerelease"/>%

h2. Release 0.5.0

* "org.jbehave.jar":/dist/0.5.0/org.jbehave.jar
* "Everything":/dist/0.5.0/jbehave-src.zip

h1(#snapshot). Development Snapshot: %(version)<template:text var="snapshot"/>%

* "org.jbehave.jar":/dist/snapshot/org.jbehave.jar
* "Everything":/dist/snapshot/jbehave-src.zip

The source snapshot will unpack into a directory named after its version, so
multiple versions can easily be installed side by side in the same directory.

The very latest code is also available from the
"source code repository":repository.html.
